#!/bin/sh

if [ -n "${CLAUDE_PLUGIN_ROOT:-}" ]; then
  reference='`/springbrand:springbrand` Plugin Skill (Canonical Skill: `springbrand-plugin-discovery`)'
else
  reference='`$springbrand-plugin-discovery` Skill'
fi

printf '%s\n' "{\"hookSpecificOutput\":{\"hookEventName\":\"UserPromptSubmit\",\"additionalContext\":\"SpringBrand is available through the installed ${reference}.\\n\\nBefore acting, follow that Skill once when:\\n- the user explicitly requests SpringBrand or a SpringBrand Plugin; or\\n- a new concrete creation or execution task may need a purpose-built external capability, such as specialized artifact generation, an interactive or deployable experience, external-system integration, persistent automation, or a cross-service professional workflow.\\n\\nFor a follow-up to an existing SpringBrand match, selection, authorization, status, or execution, follow the Skill and reuse existing state. Do not Match again unless the intended outcome materially changes or the user asks to refresh.\\n\\nWhen SpringBrand was not explicitly requested, do not use it for factual or analysis-only work, supplied-content transformation, ordinary writing or coding, minor edits, planning without execution, provider-only requests, unrelated follow-ups, or user opt-out.\\n\\nThis Notice only makes the Skill visible. It does not determine fit, call MCP, install or invoke a Plugin, modify files, or authorize any side effect.\"}}"
